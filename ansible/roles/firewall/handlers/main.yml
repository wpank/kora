---
- name: restart nftables
  ansible.builtin.systemd:
    name: nftables
    state: restarted

- name: restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
  register: restart_docker_result
  failed_when: false
